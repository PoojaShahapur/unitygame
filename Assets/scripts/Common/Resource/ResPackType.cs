namespace SDK.Common
{
    /**
     * @brief 资源包的类型
     */
    public enum ResPackType
    {
        eLevelType,     // 关卡
        eBundleType,    // 资源包，注意默认的打包 Resources 和自己真正的单独打包，都是这个类型

        eNoneType       // 默认类型
    }
}
